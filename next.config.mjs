// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // ────────────────────────────────────────────────
  // Основные настройки (чаще всего достаточно этих)
  // ────────────────────────────────────────────────

  reactStrictMode: true,

  // Оптимизация изображений (если будете использовать <Image />)
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',           // разрешить любые внешние изображения
      },
    ],
  },

  // Если используете экспериментальные функции (часто включают в 2025–2026)
  experimental: {
    // turbo: true,                   // Turbopack (если хотите попробовать)
    // serverActions: true,           // уже по умолчанию в новых версиях
    // typedRoutes: true,             // типизированные ссылки (очень полезно)
  },

  // Если планируете использовать международализацию (i18n)
  // i18n: {
  //   locales: ['ru', 'en'],
  //   defaultLocale: 'ru',
  // },

  // Переменные окружения, которые будут доступны на клиенте (префикс NEXT_PUBLIC_)
  env: {
    // NEXT_PUBLIC_APP_VERSION: '1.0.0',
    // NEXT_PUBLIC_SITE_URL: process.env.VERCEL_URL || 'http://localhost:3000',
  },

  // ────────────────────────────────────────────────
  // Настройки для продакшена / Vercel
  // ────────────────────────────────────────────────

  // Уменьшает размер бандла (часто включают)
 

  // Если приложение большое — можно включить
  // output: 'standalone',   // для docker / self-hosting

  // Если используете международализацию или много страниц
  // poweredByHeader: false,   // убрать заголовок X-Powered-By: Next.js
}

export default nextConfig